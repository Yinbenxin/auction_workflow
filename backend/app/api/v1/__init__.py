from fastapi import APIRouter

router = APIRouter()

# Future module routers will be included here, e.g.:
# from app.api.v1 import auth, users, auctions
# router.include_router(auth.router, prefix="/auth", tags=["auth"])
